OVERSEERR_API_KEY=
OVERSEERR_URL=
REQUEST_USER_ID=1
# Optional: Map Overseerr server IDs (integers) to library names (strings) for request tools.
# Use a JSON dictionary format. Keys must be integers, values must be unique strings.
# Example: LIBRARY_MAP={"0": "Movies HD", "1": "TV Shows 4K", "5": "Kids Movies"}
# Defaults to {"0": "Default Movie Library", "1": "Default TV Library"} if not set or invalid.
LIBRARY_MAP=